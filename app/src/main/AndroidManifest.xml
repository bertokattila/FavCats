<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="hu.bme.aut.bertokattila.favcats">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/cat"
        android:label="@string/app_name"
        android:roundIcon="@drawable/cat"
        android:supportsRtl="true"
        android:theme="@style/Theme.AndroidCat">
        <activity
            android:name=".activities.FavoriteCatsActivity"
            android:label="Favorite Cats"
            android:exported="true" />
        <activity
            android:name=".activities.NewCatActivity"
            android:label="New Cat"
            android:exported="true" />
        <activity
            android:name=".activities.MenuActivity"
            android:label="FavCats Main Menu"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>